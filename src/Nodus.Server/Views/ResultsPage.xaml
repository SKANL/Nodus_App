<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Nodus.Server.ViewModels"
             x:Class="Nodus.Server.Views.ResultsPage"
             Title="Live Results">

    <Grid RowDefinitions="Auto, Auto, *">
        <Label Grid.Row="0"
               Text="Incoming Votes" 
               FontSize="24" 
               FontAttributes="Bold" 
               Padding="20"/>

        <StackLayout Grid.Row="1" Orientation="Horizontal" Spacing="10" Padding="20,0,20,10">
            <Button Text="Export CSV" 
                    Command="{Binding ExportCsvCommand}" 
                    BackgroundColor="{StaticResource Secondary}"
                    TextColor="White"/>
            <Button Text="Export Excel" 
                    Command="{Binding ExportExcelCommand}" 
                    BackgroundColor="{StaticResource Secondary}"
                    TextColor="White"/>
        </StackLayout>

        <CollectionView Grid.Row="2" ItemsSource="{Binding Votes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" ColumnDefinitions="*, Auto">
                        <VerticalStackLayout>
                            <Label Text="{Binding ProjectId}" FontAttributes="Bold" />
                            <Label Text="{Binding JudgeId}" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="1" 
                               Text="{Binding Timestamp, StringFormat='{0:HH:mm:ss}'}" 
                               VerticalOptions="Center" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
