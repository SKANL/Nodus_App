// 📋 PLANTILLA — Este archivo SÍ se sube al repo.
// Para configurar el proyecto:
//   1. Copia este archivo como: AppSecrets.cs (en la misma carpeta)
//   2. Reemplaza los valores con tus credenciales reales
//   3. AppSecrets.cs está en .gitignore — nunca se sube

namespace Nodus.Shared.Config;

internal static class AppSecrets
{
    // MongoDB Atlas
    public const string MongoConnectionString =
        "mongodb+srv://USUARIO:PASSWORD@CLUSTER.mongodb.net/?appName=nodus-cluster";

    public const string MongoDatabaseName = "nodus_db";
}
